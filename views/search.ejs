<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Finder</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1 style="text-align: center">Restaurant Finder</h1>
    <form method="GET" action="/restaurants/search">
        <label for="page">Page:</label>
    <input type="number" id="page" name="page" placeholder="Page" min="1" required>
    
    <label for="perPage">Items Per Page:</label>
    <input type="number" id="perPage" name="perPage" placeholder="Items Per Page" min="1" required> 

        <label for="borough">Borough:</label>
        <input type="text" id="borough" name="borough" placeholder="Borough">

        <button type="submit">Submit</button>
    </form>

    <% if (results && results.length > 0) { %>
        <h2>Search Results</h2>
        <p>Found <%= results.length %> restaurants:</p>
        <table border="1" style="border-collapse: collapse; width: 100%; text-align: left;">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Borough</th>
                    <th>Cuisine</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach((restaurant, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= restaurant.name %></td>
                        <td><%= restaurant.borough %></td>
                        <td><%= restaurant.cuisine %></td>
                        <td>
                            <a href="/restaurants/<%= restaurant._id %>">View Details</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } else if (results) { %>
        <h2>No Results</h2>
    <% } else { %>
        <h2>Error</h2>  
    <% } %>
    
</body>
</html>
